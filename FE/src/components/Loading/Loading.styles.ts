import styled from 'styled-components';

export const LoadingContainer = styled.div`
  position: absolute;
  top: calc(50% - 2rem);
  left: calc(50% - 3rem);
  width: 6rem;
  height: 6rem;

  img {
    width: 100%;
    height: 100%;
  }
`;
